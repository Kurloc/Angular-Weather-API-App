import { ShortHandDayPipe } from './short-hand-day.pipe';

describe('ShortHandDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortHandDayPipe();
    expect(pipe).toBeTruthy();
  });
});
