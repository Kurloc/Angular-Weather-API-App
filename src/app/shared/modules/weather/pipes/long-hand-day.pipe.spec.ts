import { LongHandDayPipe } from './long-hand-day.pipe';

describe('LongHandDayPipe', () => {
  it('create an instance', () => {
    const pipe = new LongHandDayPipe();
    expect(pipe).toBeTruthy();
  });
});
